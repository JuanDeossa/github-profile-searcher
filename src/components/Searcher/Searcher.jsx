import { Stack, TextField, IconButton } from "@mui/material";

export const Searcher = () => {
  return (
    <Stack>
      <TextField></TextField>
      <IconButton></IconButton>
    </Stack>
  );
};
